/* eslint-disable */
// auto-generated by src/shader/webgpu/compile.js from *.wgsl sources
export const webgpuShaders: Record<string, string> = {
  "binary_broadcast_add_0d": "@group(0) @binding(0) var<storage, read_write> array_a: array<f32>;\n@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;\n@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;\n\nstruct Meta {\n  len: u32,\n};\n@group(0) @binding(3) var<storage, read_write> meta: Meta;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n  let i = global_id.x;\n  if (i == 0u) {\n    array_c[0] = array_a[0] + array_b[0];\n  }\n}\n",
  "binary_broadcast_add_1d": "@group(0) @binding(0) var<storage, read_write> array_a: array<f32>;\n@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;\n@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;\n\nstruct Meta {\n  len: u32,\n  outShape0: u32,\n  inAStride0: u32,\n  inBStride0: u32,\n};\n@group(0) @binding(3) var<storage, read_write> meta: Meta;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n  let len = meta.len;\n  let outShape0 = meta.outShape0;\n  let inAStride0 = meta.inAStride0;\n  let inBStride0 = meta.inBStride0;\n  for (var i = global_id.x; i < len; i = i + 4096u) {\n    let dim0 = i;\n    array_c[i] = array_a[dim0 * inAStride0] + array_b[dim0 * inBStride0];\n  }\n}\n",
  "binary_broadcast_add_2d": "@group(0) @binding(0) var<storage, read_write> array_a: array<f32>;\n@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;\n@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;\n\nstruct Meta {\n  len: u32,\n  outShape0: u32,\n  outShape1: u32,\n  inAStride0: u32,\n  inAStride1: u32,\n  inBStride0: u32,\n  inBStride1: u32,\n};\n@group(0) @binding(3) var<storage, read_write> meta: Meta;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n  let len = meta.len;\n  let outShape0 = meta.outShape0;\n  let outShape1 = meta.outShape1;\n  let inAStride0 = meta.inAStride0;\n  let inAStride1 = meta.inAStride1;\n  let inBStride0 = meta.inBStride0;\n  let inBStride1 = meta.inBStride1;\n  for (var i = global_id.x; i < len; i = i + 4096u) {\n    let dim1 = i % outShape1;\n    let dim0 = i / outShape1;\n    array_c[i] = array_a[dim0 * inAStride0 + dim1 * inAStride1] + array_b[dim0 * inBStride0 + dim1 * inBStride1];\n  }\n}\n",
  "binary_broadcast_add_3d": "@group(0) @binding(0) var<storage, read_write> array_a: array<f32>;\n@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;\n@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;\n\nstruct Meta {\n  len: u32,\n  outShape0: u32,\n  outShape1: u32,\n  outShape2: u32,\n  inAStride0: u32,\n  inAStride1: u32,\n  inAStride2: u32,\n  inBStride0: u32,\n  inBStride1: u32,\n  inBStride2: u32,\n};\n@group(0) @binding(3) var<storage, read_write> meta: Meta;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n  let len = meta.len;\n  let outShape0 = meta.outShape0;\n  let outShape1 = meta.outShape1;\n  let outShape2 = meta.outShape2;\n  let inAStride0 = meta.inAStride0;\n  let inAStride1 = meta.inAStride1;\n  let inAStride2 = meta.inAStride2;\n  let inBStride0 = meta.inBStride0;\n  let inBStride1 = meta.inBStride1;\n  let inBStride2 = meta.inBStride2;\n  for (var i = global_id.x; i < len; i = i + 4096u) {\n    let dim2 = i % outShape2;\n    var dim1 = i / outShape2;\n    let dim0 = dim1 / outShape1;\n    dim1 = dim1 % outShape1;\n    array_c[i] = array_a[dim0 * inAStride0 + dim1 * inAStride1 + dim2 * inAStride2] + array_b[dim0 * inBStride0 + dim1 * inBStride1 + dim2 * inBStride2];\n  }\n}\n",
  "binary_broadcast_add_4d": "@group(0) @binding(0) var<storage, read_write> array_a: array<f32>;\n@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;\n@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;\n\nstruct Meta {\n  len: u32,\n  outShape0: u32,\n  outShape1: u32,\n  outShape2: u32,\n  outShape3: u32,\n  inAStride0: u32,\n  inAStride1: u32,\n  inAStride2: u32,\n  inAStride3: u32,\n  inBStride0: u32,\n  inBStride1: u32,\n  inBStride2: u32,\n  inBStride3: u32,\n};\n@group(0) @binding(3) var<storage, read_write> meta: Meta;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n  let len = meta.len;\n  let outShape0 = meta.outShape0;\n  let outShape1 = meta.outShape1;\n  let outShape2 = meta.outShape2;\n  let outShape3 = meta.outShape3;\n  let inAStride0 = meta.inAStride0;\n  let inAStride1 = meta.inAStride1;\n  let inAStride2 = meta.inAStride2;\n  let inAStride3 = meta.inAStride3;\n  let inBStride0 = meta.inBStride0;\n  let inBStride1 = meta.inBStride1;\n  let inBStride2 = meta.inBStride2;\n  let inBStride3 = meta.inBStride3;\n  for (var i = global_id.x; i < len; i = i + 4096u) {\n    let dim3 = i % outShape3;\n    var dim2 = i / outShape3;\n    var dim1 = dim2 / outShape2;\n    dim2 = dim2 % outShape2;\n    let dim0 = dim1 / outShape1;\n    dim1 = dim1 % outShape1;\n    array_c[i] = array_a[dim0 * inAStride0 + dim1 * inAStride1 + dim2 * inAStride2 + dim3 * inAStride3] + array_b[dim0 * inBStride0 + dim1 * inBStride1 + dim2 * inBStride2 + dim3 * inBStride3];\n  }\n}\n",
  "binary_elementwise_add": "@group(0) @binding(0) var<storage, read_write> array_a: array<f32>;\n@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;\n@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;\n\nstruct Meta {\n  len: u32,\n};\n@group(0) @binding(3) var<storage, read_write> meta: Meta;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n  let len = meta.len;\n  for (var i = global_id.x; i < len; i = i + 4096u) {\n    array_c[i] = array_a[i] + array_b[i];\n  }\n}\n",
  "conv_bias": "@group(0) @binding(0) var<storage, read_write> array_i: array<f32>;\n@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;\n@group(0) @binding(2) var<storage, read_write> array_o: array<f32>;\n\nstruct Meta {\n  batch: i32,\n  chOut: i32,\n  outarea: i32,\n};\n@group(0) @binding(3) var<storage, read_write> meta: Meta;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n  let batch = meta.batch;\n  let chOut = meta.chOut;\n  let outarea = meta.outarea;\n  let total = batch * chOut * outarea;\n  for (var idx = i32(global_id.x); idx < total; idx = idx + 4096) {\n    let x = idx % outarea;\n    var c = idx / outarea;\n    // let b = c / chOut;\n    c = c % chOut;\n    array_o[idx] = array_i[idx] + array_b[c];\n  }\n}\n",
  "conv_im2col": "@group(0) @binding(0) var<storage, read_write> array_x: array<f32>;\n@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;\n\nstruct Meta {\n  group: i32,\n  batch: i32,\n  outShape0: i32,\n  outShape1: i32,\n  chInPerGroup: i32,\n  kernelShape0: i32,\n  kernelShape1: i32,\n  strides0: i32,\n  strides1: i32,\n  pads0: i32,\n  pads1: i32,\n  dilations0: i32,\n  dilations1: i32,\n  inShape0: i32,\n  inShape1: i32,\n  chIn: i32,\n};\n@group(0) @binding(2) var<storage, read_write> meta: Meta;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n  let group = meta.group;\n  let batch = meta.batch;\n  let outShape0 = meta.outShape0;\n  let outShape1 = meta.outShape1;\n  let chInPerGroup = meta.chInPerGroup;\n  let kernelShape0 = meta.kernelShape0;\n  let kernelShape1 = meta.kernelShape1;\n  let strides0 = meta.strides0;\n  let strides1 = meta.strides1;\n  let pads0 = meta.pads0;\n  let pads1 = meta.pads1;\n  let dilations0 = meta.dilations0;\n  let dilations1 = meta.dilations1;\n  let inShape0 = meta.inShape0;\n  let inShape1 = meta.inShape1;\n  let chIn = meta.chIn;\n  let total = group * batch * outShape0 * outShape1 * chInPerGroup * kernelShape0 * kernelShape1;\n  for (var idx = i32(global_id.x); idx < total; idx = idx + 4096) {\n    var ky = idx / kernelShape1;\n    let kx = idx % kernelShape1;\n    var ci = ky / kernelShape0;\n    ky = ky % kernelShape0;\n    var ox = ci / chInPerGroup;\n    ci = ci % chInPerGroup;\n    var oy = ox / outShape1;\n    ox = ox % outShape1;\n    var b = oy / outShape0;\n    oy = oy % outShape0;\n    var g = b / batch;\n    b = b % batch;\n\n    let iny = oy * strides0 - pads0 + ky * dilations0;\n    let inx = ox * strides1 - pads1 + kx * dilations1;\n    var v: f32;\n    if (iny >= 0 && iny < inShape0 && inx >= 0 && inx < inShape1) {\n      v = array_x[u32(((b * chIn + g * chInPerGroup + ci) * inShape0 + iny) * inShape1 + inx)];\n    } else {\n      v = 0.0;\n    }\n    array_y[idx] = v;\n  }\n}\n",
  "conv_matmul": "@group(0) @binding(0) var<storage, read_write> array_i: array<f32>;\n@group(0) @binding(1) var<storage, read_write> array_w: array<f32>;\n@group(0) @binding(2) var<storage, read_write> array_t: array<f32>;\n\nstruct Meta {\n  group: i32,\n  bout: i32,\n  chOutPerGroup: i32,\n  cinkhkw: i32,\n};\n@group(0) @binding(3) var<storage, read_write> meta: Meta;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n  let group = meta.group;\n  let bout = meta.bout;\n  let chOutPerGroup = meta.chOutPerGroup;\n  let cinkhkw = meta.cinkhkw;\n  let total = group * bout * chOutPerGroup;\n  for (var idx = i32(global_id.x); idx < total; idx = idx + 4096) {\n    let x = idx % chOutPerGroup;\n    var y = idx / chOutPerGroup;\n    let g = y / bout;\n    y = y % bout;\n    var s = 0.0;\n    for (var ip = 0; ip < cinkhkw; ip = ip + 1) {\n      s = s + array_i[(g * bout + y) * cinkhkw + ip] * array_w[(g * chOutPerGroup + x) * cinkhkw + ip];\n    }\n    array_t[idx] = s;\n  }\n}\n",
  "conv_transpose": "@group(0) @binding(0) var<storage, read_write> array_t: array<f32>;\n@group(0) @binding(1) var<storage, read_write> array_o: array<f32>;\n\nstruct Meta {\n  group: i32,\n  batch: i32,\n  outarea: i32,\n  chOutPerGroup: i32,\n};\n@group(0) @binding(2) var<storage, read_write> meta: Meta;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n  let group = meta.group;\n  let batch = meta.batch;\n  let outarea = meta.outarea;\n  let chOutPerGroup = meta.chOutPerGroup;\n  let total = group * batch * chOutPerGroup * outarea;\n  for (var idx = i32(global_id.x); idx < total; idx = idx + 4096) {\n    let x = idx % outarea;\n    var c = idx / outarea;\n    var g = c / chOutPerGroup;\n    c = c % chOutPerGroup;\n    let b = g / group;\n    g = g % group;\n    array_o[idx] = array_t[((g * batch + b) * outarea + x) * chOutPerGroup + c];\n  }\n}\n",
  "gemm": "@group(0) @binding(0) var<storage, read_write> array_a: array<f32>;\n@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;\n@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;\n@group(0) @binding(3) var<storage, read_write> array_y: array<f32>;\n\nstruct Meta {\n  M: u32,\n  N: u32,\n  K: u32,\n  strideA0: u32,\n  strideA1: u32,\n  strideB0: u32,\n  strideB1: u32,\n  strideC0: u32,\n  strideC1: u32,\n  alpha: f32,\n  beta: f32,\n};\n@group(0) @binding(4) var<storage, read_write> meta: Meta;\n\n@compute @workgroup_size(8, 8, 1)\nfn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n  let M = meta.M;\n  let N = meta.N;\n  let K = meta.K;\n  let strideA0 = meta.strideA0;\n  let strideA1 = meta.strideA1;\n  let strideB0 = meta.strideB0;\n  let strideB1 = meta.strideB1;\n  let strideC0 = meta.strideC0;\n  let strideC1 = meta.strideC1;\n  let alpha = meta.alpha;\n  let beta = meta.beta;\n  for (var x = global_id.x; x < N; x = x + 256u) {\n    for (var y = global_id.y; y < M; y = y + 256u) {\n      var sum = 0.0;\n      for (var k = 0u; k < K; k = k + 1u) {\n        sum = sum + array_a[y * strideA0 + k * strideA1] * array_b[k * strideB0 + x * strideB1];\n      }\n      array_y[x + y * N] = sum * alpha + array_c[y * strideC0 + x * strideC1] * beta;\n    }\n  }\n}\n",
  "relu": "@group(0) @binding(0) var<storage, read_write> array_a: array<f32>;\n@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;\n\nstruct Meta {\n  len: u32,\n};\n@group(0) @binding(2) var<storage, read_write> meta: Meta;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n  let len = meta.len;\n  for (var i = global_id.x; i < len; i = i + 4096u) {\n    array_b[i] = max(array_a[i], 0.0);\n  }\n}\n",
};
